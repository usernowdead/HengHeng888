# คู่มือเริ่มต้นใช้งาน Premium App Store (Quick Start)

## ✅ สิ่งที่ติดตั้งแล้ว
- ✅ Frontend dependencies (npm packages)
- ✅ Docker Desktop (พร้อมใช้งาน)

## ❌ สิ่งที่ยังต้องติดตั้ง

### 1. Rust & Cargo (จำเป็นสำหรับ Backend)
**วิธีติดตั้ง:**
- ดาวน์โหลดจาก: https://rustup.rs/
- Windows: ดาวน์โหลด `rustup-init.exe` แล้วรัน
- หรือใช้คำสั่ง PowerShell:
  ```powershell
  Invoke-WebRequest -Uri https://win.rustup.rs/x86_64 -OutFile rustup-init.exe
  .\rustup-init.exe
  ```
- **หลังจากติดตั้งเสร็จ ต้องเปิด PowerShell/Terminal ใหม่**

### 2. sqlx-cli (สำหรับ Database Migrations)
หลังจากติดตั้ง Rust แล้ว:
```powershell
cargo install sqlx-cli
```

## 🚀 ขั้นตอนการเริ่มใช้งาน

### ขั้นตอนที่ 1: Start Database
เปิด PowerShell ที่โฟลเดอร์โปรเจกต์ แล้วรัน:
```powershell
.\setup-database.ps1
```

หรือใช้ Docker แบบ manual:
```powershell
docker-compose up -d
```

**ตรวจสอบว่า container ทำงาน:**
```powershell
docker ps
```

### ขั้นตอนที่ 2: รัน Database Migrations
**รอให้ติดตั้ง Rust เสร็จก่อน** แล้วรัน:
```powershell
cd backend
sqlx migrate run --database-url postgresql://appstore_user:appstore_password@localhost:5432/premium_appstore
```

### ขั้นตอนที่ 3: Start Backend Server
เปิด PowerShell ใหม่ (Terminal 1):
```powershell
.\start-backend.ps1
```

หรือ manual:
```powershell
cd backend
cargo run
```

Backend จะรันที่ `http://localhost:3001`

### ขั้นตอนที่ 4: Start Frontend Server
เปิด PowerShell ใหม่ (Terminal 2):
```powershell
.\start-frontend.ps1
```

หรือ manual:
```powershell
cd frontend
npm run dev
```

Frontend จะรันที่ `http://localhost:3000`

## 📝 หมายเหตุสำคัญ

1. **ต้องติดตั้ง Rust ก่อน** ถึงจะรัน Backend ได้
2. **ต้องเปิด Docker Desktop** ก่อนรัน database setup
3. **Backend ต้องรันก่อน Frontend** เพื่อให้ API พร้อมใช้งาน
4. ไฟล์ `.env` และ `.env.local` จะถูกสร้างอัตโนมัติเมื่อรันสคริปต์

## 🔍 ตรวจสอบการทำงาน

1. **Database**: เปิดเบราว์เซอร์ไปที่ `http://localhost:3001/api/banners` ควรได้ JSON response
2. **Frontend**: เปิดเบราว์เซอร์ไปที่ `http://localhost:3000` ควรเห็นหน้าแรก
3. **Admin**: เปิดเบราว์เซอร์ไปที่ `http://localhost:3000/admin` ควรเห็น Admin Dashboard

## ⚠️ Troubleshooting

### Docker ไม่ทำงาน
- เปิด Docker Desktop
- รอให้ Docker Engine เริ่มทำงาน (ไอคอน Docker ใน System Tray ต้องเป็นสีเขียว)
- ลองรัน `docker ps` อีกครั้ง

### Cargo/Rust ไม่พบ
- ตรวจสอบว่า Rust ติดตั้งแล้ว: `rustc --version`
- **สำคัญ**: ต้องเปิด PowerShell/Terminal ใหม่หลังจากติดตั้ง Rust
- ตรวจสอบ PATH environment variable

### Port ถูกใช้แล้ว
- ถ้า port 3000 หรือ 3001 ถูกใช้:
  - Frontend: แก้ไข `package.json` หรือใช้ `npm run dev -- -p 3002`
  - Backend: แก้ไข `PORT` ใน `backend/.env`

### Database connection error
- ตรวจสอบว่า PostgreSQL container ทำงาน: `docker ps`
- ตรวจสอบ `DATABASE_URL` ใน `backend/.env`
- ตรวจสอบว่า migrations รันแล้ว: เช็คว่า tables ถูกสร้าง


